using System;

namespace ConsoleApp2
{
    class Program
    {
        static void Main(string[] args)
        {
            /* Programa que declare dos variables de tipo entero (con el nombre que quieras) y les asigne un valor (los valores que quieras). Después 
            que muestre la suma de ambas por pantalla con la frase "La suma es..."*/

            int num1 = 6;

            int num2 = 7;

            int resultado = num1 + num2;

            Console.WriteLine($"El resultado es {resultado}");

        }
    }
}